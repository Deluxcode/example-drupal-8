{{ attach_library('gfest/owlcarousel') }}
<div{{ attributes.addClass('partners_slider', 'owl-carousel', 'owl-theme', 'js-partners_slider') }}>
  {% for item in items %}
    <div{{ item.attributes.addClass('slider_item', 'partner_logo') }}>{{ item.content }}</div>
  {% endfor %}
</div>
