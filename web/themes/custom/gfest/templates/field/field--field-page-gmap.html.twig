{% for item in items %}
  <div{{ attributes.addClass('map_section', 'section') }}>
    <iframe src="{{ item.content }}" width="100%" height="450" style="border:0" allowfullscreen></iframe></div>
{% endfor %}
