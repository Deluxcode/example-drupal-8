{% extends "block.html.twig" %}
{% block content %}
  {% if site_logo %}
    <a class="header_logo" href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home">
      <img src="{{ site_logo }}" alt="{{ 'Home'|t }}" />
    </a>
  {% endif %}
{% endblock %}